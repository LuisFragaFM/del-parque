export interface Condomino {
  id: string;
  name: string;
  street: string;
  houseNumber: string;
  telephoneNumber: string;
  email: string;
  emergencyNumber: string;
  paidStatus: boolean;
  userId: string;
  role: string;
}
